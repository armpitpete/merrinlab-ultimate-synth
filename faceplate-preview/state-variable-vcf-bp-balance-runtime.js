(() => {
  "use strict";

  // Retained as a compatibility stub for older cached pages. Band-pass
  // calibration now belongs to effects-output-graph.js and never patches
  // AudioContext or AudioNode prototypes.
})();
