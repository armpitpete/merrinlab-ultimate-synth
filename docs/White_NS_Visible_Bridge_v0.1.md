# White NS Visible Bridge v0.1

Issue #49 records the first visible noise-level bridge.

Active visible control:

- Mixer White NS Level -> `whiteNoiseLevel`

Implementation file:

```text
faceplate-preview/vco1-visible-controls.js
```

Scope:

- visible White NS Level only
- no visible Noise Type selector yet
- no noise colour change
- no socket activation
- no external input
- no routing change

Good enough:

```text
Visible White NS Level controls the existing selected-noise level only.
```
