# First Voice Panel Docking v0.1

Issue #52 fixes a layout problem found during visible-interface testing.

Problem:

- the First Voice control panel was fixed over the page
- some visible synth controls could be hidden behind it

Fix:

- dock the First Voice panel below the synth preview
- keep all First Voice controls available
- do not change audio behaviour
- do not change visible synth control behaviour

Good enough:

```text
First Voice controls no longer cover the visible synth interface.
```
