# Lowpass Initial Cutoff Visible Bridge v0.1

Issue #50 records the first visible lowpass filter bridge.

Active visible control:

- Lowpass Initial Cutoff -> `cutoff`

Implementation file:

```text
faceplate-preview/vco1-visible-controls.js
```

Scope:

- visible Initial Cutoff only
- no Resonance change yet
- no AR Mod
- no LFO-1 Mod
- no Ext CV
- no socket activation
- no routing change

Good enough:

```text
Visible Lowpass Initial Cutoff controls the existing filter cutoff state only.
```
